# Core (required)
pandas>=2.0
numpy>=1.24

# Live market data (optional but recommended; engine falls back to synthetic)
yfinance>=0.2.40

# Live NSE option chain for Max Pain / PCR / IV surface (optional; falls back
# to synthetic OI if unreachable). yfinance already pulls in requests.
requests>=2.28

# Dashboard (optional)
streamlit>=1.30
